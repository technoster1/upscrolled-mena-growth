export type ScrollPack = {
  id: string;
  title: string;
  theme: string;
  description?: string;
};

/** Scroll Pack concepts for curated creator and community discovery. */
export const scrollPacks: ScrollPack[] = [];
